import { Settings } from 'lucide-react'
import { PlaceholderPage } from '@components/dashboard/shared/PlaceholderPage.jsx'

export function SettingsPage() {
	return (
		<PlaceholderPage
			title="Settings"
			subtitle="Customize your experience, manage preferences, notification settings, and account security."
			icon={Settings}
		/>
	)
}
